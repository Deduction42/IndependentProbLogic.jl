1.  Build out other operations
    -   min/max operations (for weakest/strongest link logic)
    -   ∧ ∨ (\wedge \vee normalized "and/or" to prevent zero-bias from multiple observations)
        -   divide log-likelihood by number of observations
        -   ∧(x...) = sum(xi->fillnan(xi,0), x)/sum(!isnan, x)

2.  Build out raw likelihood operations 

3.  Implement "likelihood(threshold, x)" and loglikelihood(threshold, x)" functions

4.  Test autodiff/ML compatibility